services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: bookcafe-backend
    restart: unless-stopped
    environment:
      - PORT=4000
      - NODE_ENV=production
      - DB_PATH=/app/data/bookcafe.db
      - JWT_SECRET=supersecret-bookcafe-token
      - ADMIN_KEY=akm-demo-key
    volumes:
      - bookcafe_data:/app/data
    networks:
      - bookcafe-net

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: bookcafe-frontend
    restart: unless-stopped
    depends_on:
      - backend
    networks:
      - bookcafe-net

volumes:
  bookcafe_data:
    name: bookcafe_data

networks:
  bookcafe-net:
    driver: bridge
